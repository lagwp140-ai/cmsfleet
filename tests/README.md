# Tests

Cross-package and cross-service tests should live here once the first features exist.

## Suggested Areas

- `contract/`: API contract tests
- `integration/`: database and worker integration tests
- `e2e/`: browser or workflow-level tests

Unit tests should remain close to the code they verify inside each package.