# Backend

Node.js backend packages live here.

## Intended Contents

- `api/`: main HTTP API for operator-facing workflows and transactional business logic
- `config-runtime/`: shared configuration loader and schema validation for Node services

Add more backend packages only when they represent distinct deployable units or shared backend libraries with clear ownership.